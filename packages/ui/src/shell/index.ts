/**
 * The §8 screen-shell inventory — VALUE exports only, same registry contract as
 * `components/index.ts`: everything here needs a state entry in `gallery/registry.ts`.
 */
export { AppShell } from './AppShell.js';
export { AvatarButton } from './AvatarButton.js';
export { SyncChip } from './SyncChip.js';
