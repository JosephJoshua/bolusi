// The grant-all permission evaluator now lives in the platform-free shared rig
// (@bolusi/test-support/chaos, task 181). Re-exported unchanged.
export { buildGrantAllEvaluator } from '@bolusi/test-support/chaos';
