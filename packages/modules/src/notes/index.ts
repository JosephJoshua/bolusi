// Platform-free notes module manifest placeholder — real defineModule lands in task 25.
// Server and harness may import ONLY this subpath, never ./screens (08 §3.2).
export const NOTES_MODULE_ID = 'notes' as const;
