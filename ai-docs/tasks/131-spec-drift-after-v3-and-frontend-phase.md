# TASK 131 — spec drift after the v3 payload and the frontend phase: docs and code comments describe a system that no longer exists

**Priority:** MEDIUM — a stale spec is a decoy, and this repo's §2.11 record shows decoys get trusted. Two of these are the "comment was the guard" shape on the very files whose correctness they describe.
**Depends on:** 120, 119, 96, 118
**Blocks:** —
**SEC ids owned by THIS task:** none.
**Filed by:** QA spec-verify sweep, 2026-07-22 (items D2/D3/D5/D8/D9 + nits).

>**PARTIAL — items 1, 2, 4, 6, 7 and 8 are DONE** (only items 3 + 5 remain, both contended-file-gated): `01-domain-model.md` now documents v3 `{title, body, mediaRef}` with the `mediaSha256`/`mediaMime` projection columns and the "every foldable version needs a retained payload schema" rule that task 127 turns into code; `10-db-schema.md` §8 (PG) and §9.6 (SQLite) now carry both columns with their migration ids (`0010` server / `002` client). Item 4 (applier header) is done — `packages/modules/src/notes/applier.ts`'s header no longer says v3 REJECTS LOUDLY; it now documents that the applier folds all three versions and that per-version payload VALIDATION is task 127's separate `payloadByVersion` layer. **Item 6's `testing-guide.md:137` sub-point was resolved by task 174 (2026-07-28); item 8 (08-stack §7 record 4 SQLCipher drift) done 2026-07-28.** Items 3, 5, 7 and item 6's remaining sub-points (`04-module-contract.md:42` schemaVersion example, `media/client.ts` "NOT REAL YET" block) remain open — several touch contended files (App.tsx per task 133; media/client.ts per task 140) and should land after those settle.
>
>**DONE 2026-08-04 (deps 133/140/119/25 all settled). Verifying ground truth first surfaced that several item premises had themselves drifted (T-11/T-16):**
> - **Item 3 — CORRECTED, NOT a decoy.** The push-muting model is LIVE, not dead: `channelImportance`/`categoryNameKey` (`settings/model.ts`) drive `createNotificationChannels` at boot (`bootstrap/notifications.ts` — `androidImportance(muted) === 'min'` sets each Android channel's importance, `categoryNameKey` its name). `model.test.ts` asserts live behavior. D18/task 59 removed the app-controlled mute TOGGLE (muting is now an OS-settings deep-link), but the mapping mute→channel-importance survives as the initial channel-setup mechanism. So there was nothing dead to delete — the "green-guarded decoy" premise was stale. Left as-is.
> - **Item 5 — DONE.** `App.tsx` (now ~:637): the comment said `props.notes` is `undefined` "today" (pre-session-wiring); task 119 wired it, and `undefined` now means only the no-runtime case (unenrolled / Node harness, per `CaptureHost`). Comment corrected; the defensive branch is live and stays.
> - **Item 6a — DONE.** `04-module-contract.md` §3 example bumped from the superseded `schemaVersion: 2` (the task said v1; it had already moved to v2) to the current **v3** (`mediaRef`, 2-entry `payloadByVersion`), matching the real `notes.note_created` it names.
> - **Item 6b — DONE (re-corrected after review 2026-08-04).** First pass got this wrong too — it described the pre-task-130 world (the false-claim class, caught by the reviewer). Ground truth: **task 130 wired the capture host end-to-end** — `index.ts` binds `createExpoCapturePlatform()`, `Root.tsx` passes the real `capture.capturePhoto` into `createNotes`, `NoteEditor.tsx` calls it, and `loadThumbnail` binds to `createNotesThumbnailLoader` (task 120). `UNWIRED_NOTES_MEDIA.capturePhoto` (rejects, never a fake `null`) is only the `media === null` / no-camera-build fallback (`bootstrap/notes.ts`). So the whole "NOT REAL YET" bucket was stale; it is retired, and the honest residual — the shutter producing bytes on real hardware — moved to "NOT VERIFIABLE HERE" (D12/D13). This also resolves the index.ts contradiction Drift item 6 named.
> - **Item 7** was already marked DONE above (api/04-push cross-tenant collision, task 156).

## Drift (each with file:line; fix the DOC or the CODE, whichever is wrong — and say which)

1. **`01-domain-model.md:288,298,302`** documents `note_created` at **v1/v2 only** and says `mediaId` was "Introduced by payload `schemaVersion: 2`" and "The v1→v2 bump … is deliberate". Task 120 made **v3** `{title, body, mediaRef}` current and added `mediaSha256`/`mediaMime` to the Note projection. `applier.ts:1` cites this section as "the authoritative type list" — so the authority is now wrong.
2. **`10-db-schema.md:609-623` (PG §8) and `:902-916` (SQLite §9.6)** list `media_id` only, commented "schemaVersion 2 payloads". Migrations `0010`/`002` added `media_sha256` + `media_mime` (`generated/db.ts:170` has them). 10-db's own change-control rule is "change this doc first, then write the migration", and `0010`'s header cites §8 as its source — the section that never got the columns.
3. **`apps/mobile/src/screens/settings/model.ts:52-62,117-124`** still ships the SUPERSEDED push-muting model and cites api/04-push §5 for it ("muting is expressed as channel IMPORTANCE"; `setMuted` → `setChannelImportance`). D18 §1 / task 59 replaced that with the OS-settings deep-link, and §5 now says explicitly it is "not a switch the app sets". Dead in production (the shipping screen renders the correct row) — but `model.test.ts:94` **asserts the superseded rule**, so the decoy is green-guarded. Delete the dead model and its test, or state why it survives.
4. **`packages/modules/src/notes/applier.ts:11-20,146`** — header says "THE v1↔v2 SEAM … **A v3-or-unknown version REJECTS LOUDLY (throws)**" and `:146` says "Fold `notes.note_created` (v1 or v2)". The code at `:133-138` **folds v3**. The comment describes the opposite of the function beneath it.
5. **`apps/mobile/App.tsx:281-283`** — "Until the shell is session-wired (`props.notes` is `undefined` today)…". Task 119 wired it (`Root.tsx:428`).
6. Cosmetic: `04-module-contract.md:42` shows `schemaVersion: 1` in the §3 example a new module author copies; ~~`testing-guide.md:137` says "v2 after" the cutover (reads as if v2 were head)~~ **— DONE by task 174 (2026-07-28): the whole generator-cutover sentence was removed and now points at `migration.test.ts`, so the "v2 as head" reading is gone**; `media/client.ts:24-30`'s "NOT REAL YET" block is stale for `loadThumbnail` (120 wired it) and is contradicted by an adjacent paragraph in `index.ts`.
7. ~~**`api/04-push §2` has no row for a cross-tenant token collision.**~~ **— DONE 2026-07-28 (task 156's push-doc wave):** api/04-push §2 now documents the `403 PERMISSION_DENIED` fail-closed behaviour (verified at `routes/push.ts` `isUniqueViolation`), cross-referencing **security-guide §2.2 documented exception 2**. Note the task's proposed argument ("colliding value is a secret token") was SUPERSEDED: the accepted justification (D22 §2 / §2.2, SEC-TENANT-04) is the **30/day probe budget**, NOT token secrecy — the doc records the correct one.
8. **`08-stack-and-repo.md` §7 record 4 (line 351)** still says *"SQLCipher `4.14.0` … the podspec confirms the `package.json` `op-sqlite` config block drives `sqlcipher`/`performanceMode`."* D22 (task 148) removed SQLCipher from **both** platforms (`op-sqlite.ts:123` passes no `encryptionKey`; at-rest is app-layer AES-256-GCM now). So the record over-claims a dead mechanism: only `performanceMode` remains in the `op-sqlite` block, and whether it is discovered on iOS is unproven (iOS is not scaffolded). Correct the record to drop the SQLCipher engine line and reframe the discovery claim as `performanceMode`-only + iOS-unverified. *(Rehomed from task 151, whose security premise was refuted on 2026-07-25; this is the one live residual it left.)* **— DONE 2026-07-28: §7 record 4 now states the config block drives only `performanceMode`, sqlcipher OFF (D22), and the podspec/iOS discovery is unverified (iOS unscaffolded). The bundled-version bootstrap fact was kept (op-sqlite vendors both in `cpp/`).**

## Deliverable
Reconcile each. Where the doc is authority (01-domain-model, 10-db-schema), update the doc; where a comment lies about its own code, fix the comment (or the code if the comment was right). For item 3, remove the dead model + its guarding test rather than leaving a green-guarded decoy.
