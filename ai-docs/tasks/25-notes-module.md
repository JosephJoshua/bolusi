# TASK 25 — notes-reference-module (ops v1+v2, commands, projections, queries, conflicts) — DATA LAYER

**Status:** done
**Depends on:** 11, 18, 24

> **SCOPE (2026-07-17 — screens carved to [[96-notes-module-screens]] per D17 "frontend is later").** THIS task delivers the **platform-free data layer** — `manifest`/`operations` (v1+v2)/`applier`/`commands`/`queries`/`conflict-checks`, the `SERVER_MODULES` registration (falsified), the `{id,en}.json` i18n **catalogs**, and every **non-screen** 04 §8 box (manifest correctness, schemaVersion-2 migration, command units, permission-denial at the command/query level — NOT the rendered Unauthorized state, convergence/reorder/replay, two-device offline merge, conflict decls, full rebuild, the **headless** live-query invalidation test, and the op-level media-attach assertion) plus the task-26 chaos prerequisites (`edit_count`, v1→v2 cutover seam). It is the critical-path unblocker for task 26 and must ship complete and falsified.
> **Do NOT build the three screens here** (`NotesList`/`NoteEditor`/`NoteDetail`) — task 96 owns them and every screen box: the four §5 states incl. the rendered Unauthorized state, ConfirmSheet archive, optimistic save, the rejected-op danger banner, the **mounted-screen** live-update re-render, the NoteDetail thumbnail / NotesList glyph, and the i18n live-switch demo. The `./notes/screens` package export may stay unshipped until 96, OR land here as an empty placeholder if the manifest split needs it — your call, but no screen COMPONENTS. Where an Acceptance bullet below names a screen (e.g. "NotesList renders the §5 Unauthorized state", "NoteDetail renders the thumbnail", the Screens box, the i18n live-switch demo), that clause belongs to **task 96**; deliver its data-layer half here (the `PERMISSION_DENIED` from the query, the v2 payload carrying the `mediaId`, the catalog completeness) and leave the rendering to 96.

## Goal
Deliver the `notes` reference module in `@bolusi/modules` — the v0 exit-criterion module that proves every contract seam per 04-module-contract §8, exactly as checklisted there. One `defineModule` manifest (imported by both `apps/mobile` and `apps/server` via the `@bolusi/modules/notes` subpath): op registry `notes.note_created` (schemaVersion 1 **and** 2 — v2 adds `mediaId`), `notes.note_body_edited` (conflict decl `{key: 'note.body', severity: 'minor'}`), `notes.note_archived`; commands `createNote` / `editNoteBody` / `archiveNote` gated by `notes.create|edit|archive`; the `notes` projection (manifest columns = 10-db DDL verbatim, incl. `edit_count`); queries `listNotes` (cursor pagination, archived filter) + `getNote` gated by `notes.read`; and the Rule-2 invariant check `notes:edit_after_archive` (significant) exported for the server conflict pipeline. Screens (`@bolusi/modules/notes/screens`, wired into task-24 navigation): NotesList, NoteEditor (create/edit + camera media attach via task-18 media-client), NoteDetail. i18n complete: the module's `{id,en}.json` catalogs carry every `notes.*` key from ui-labels; zero hardcoded strings. No new server endpoints, no new state machines — registration only; harness-scale chaos runs land in task 26, on-device perf gates in task 27.

## Docs to read
- `04-module-contract.md` — §3 (op registry, `.strict()`, `reversal`, schemaVersion rules), §4 (applier rules, order-independence, rebuild, §4.4 manifest shape — `edit_count` declared), §5 (commands, runtime sequence, DomainError registry), §6 (queries, cursor pagination, gating in handler), §7 (screens, live-query invalidation rule), §8 (the checklist this task exists to satisfy).
- `01-domain-model.md` — §9 (notes projection fields, op-type table incl. v1/v2 payloads, commands/queries/permissions, archived-edit semantics); §8.1–8.3 only as referenced by §9 (conflict declaration shape, Rule-2 `notes:edit_after_archive`, minor vs significant lifecycles).
- `10-db-schema.md` — the two `notes` DDL blocks (Postgres "notes (reference-module projection)" + SQLite §9.6) verbatim, incl. `edit_count` comments and the `idx_notes_store_created` / `idx_notes_created` cursor indexes.
- `design-system.md` — §8.6 (the three notes screens, normative shapes), §5 (four mandatory screen states; unauthorized ≠ empty), §3.5/§3.6/§3.10 as referenced by §8.6 (sync chip, danger banner, ConfirmSheet for archive).
- `testing-guide.md` — §3.2 (the two testability requirements on THIS module: `edit_count`, v1→v2 cutover seam), §3.4 (convergence-oracle column rules the manifest must satisfy); CHAOS-01/06/07/08 rows only to see what task 26 will demand of this workload.
- `ui-labels.md` — the `notes.*` table (exact ID/EN strings); `07-i18n.md` §catalog layout only for file location + namespace rule (`packages/modules/notes/i18n/{id,en}.json`, namespace = module id).

## Skills
- `superpowers:test-driven-development` — always.
- `frontend-design:frontend-design` — the three screens are the ergonomics testbed for all future module screens (design-system §8.6).
- `superpowers:verification-before-completion` — run suites + the on-device demo before claiming done.
- Worktree isolation per CLAUDE.md §2.3 — first step: `git branch --show-current`; STOP if on main.

## Files / modules touched
- `packages/modules/notes/src/` — `manifest.ts` (defineModule), `operations.ts` (3 op types, v1+v2 payload schemas, conflict decl, reversal docs), `applier.ts` (dialect-neutral, handles both schemaVersions forever), `commands.ts`, `queries.ts`, `conflict-checks.ts` (`notes:edit_after_archive` check fn, platform-free, consumed by the task-17 server pipeline's registration point).
- `packages/modules/notes/screens/` — `NotesList.tsx`, `NoteEditor.tsx`, `NoteDetail.tsx` (RN, `useQuery`/`useCommand` only, task-18 capture flow for attach).
- `packages/modules/notes/i18n/id.json`, `en.json` — every `notes.*` key from ui-labels, both locales.
- `packages/modules/package.json` — `exports` for `./notes` (platform-free manifest) and `./notes/screens` (08-stack §3.2 subpath split).
- `packages/modules/test/` — module test suites incl. the dual-dialect applier run (better-sqlite3 + PGlite, 08-stack §5.4).
- `apps/mobile` — register the notes module + routes in task-24 navigation; no shell rework.
- `apps/server` — add the notes manifest to the module registration list; nothing else.
- **Not touched:** `@bolusi/core`, `@bolusi/schemas` (contended). If building notes reveals contract friction, STOP: 04 §8 says the doc changes first — that is a separate serialized task, not a side effect here.

## Acceptance
Observable: `pnpm typecheck`, `pnpm lint`, `pnpm test` green including the new `packages/modules/test` suites; the dev build runs all three screens end-to-end. Every 04 §8 checkbox lands as a named test or a verified demo, as follows.

- **Manifest correctness (04 §4.4 / oracle prerequisite):** test asserting the projection manifest declares exactly the 10-db `notes` columns, in DDL order, incl. `edit_count`; dual-dialect suite runs the applier against better-sqlite3 AND PGlite and asserts identical normalized table contents for the same op set (04 §2).
- **schemaVersion-2 mid-history migration (04 §8 box 1):** seeded history with `note_created` v1 payloads before a cutover index and v2 (`mediaId`) after (testing-guide §3.2.2); incremental apply AND full rebuild both yield `media_id = null` for v1 notes and the attached id for v2 notes; a v3 or unknown-version op → applier rejects loudly (no silent skip).
- **Command unit tests:** `createNote` happy path emits v2 `note_created` with runtime-completed envelope; empty title → `VALIDATION_FAILED`; unknown payload key → rejected (`.strict()`); `editNoteBody` on a locally-archived note → `DomainError('INVALID_TRANSITION')` (01 §9); `archiveNote` on an already-archived note → `INVALID_TRANSITION` (archived is terminal, no unarchive); any command on a nonexistent note id → `ENTITY_NOT_FOUND`; handlers contain no `Date.now()`, no I/O beyond `ctx` (04 §5.2).
- **Permission-denial path (04 §8 box 2 — this surface's adversarial floor, ships before review per CLAUDE.md §2.5):** a zero-grant user: every command → `DomainError('PERMISSION_DENIED')` + a denial op emitted through the task-09/10 enforcement point; `listNotes`/`getNote` → explicit `PERMISSION_DENIED`, never `{ rows: [] }`; NotesList renders the §5 Unauthorized state (distinct from Empty) and hides the create-CTA without `notes.create`. No SEC-\* ids name this module (security-guide has no notes family; SEC-OPLOG/SYNC/MEDIA belong to tasks 07/16/19) — these denial tests are the mandated substitute and must exist before review-wave.
- **Convergence, reorder/replay (04 §8 box 3):** feed one seeded op set (multi-device, same-entity contention) in PRNG-shuffled orders through the task-08 engine; final `notes` table contents identical across orders and equal to the canonical-order fold; assert BOTH §4.2 paths were hit (head-apply and re-fold counters > 0, per CHAOS-01's inconclusiveness rule). Replay/idempotency: re-applying an already-applied op is a no-op with `edit_count` unchanged (the CHAOS-06 assertion at module scale).
- **Two-device offline merge (04 §8 box 6):** devices A and B share a synced note, both offline, both edit the body; deliver in both arrival orders → identical projections on both, winner = canonically-later op under `(timestamp, deviceId, seq)`, `edit_count` counts both edits (no lost write). Timestamp-tie sub-case: greater `deviceId` wins deterministically.
- **Conflict declarations:** manifest test — `note_body_edited` carries `{key: 'note.body', severity: 'minor'}`; `note_created`/`note_archived` carry none. `notes:edit_after_archive` check fn: given a folded state where the note is archived, an accepted `note_body_edited` → returns a significant conflict; not archived → no conflict. (Full server-pipeline + acknowledge flow = task 17; harness-scale CHAOS-07 incl. sub-case iii = task 26 — this task must leave that workload ready: decls, check fn, `edit_count`.)
- **Full rebuild (04 §8 box 4):** Node test — drop notes projection tables, replay seeded realistic history, table contents equal the never-rebuilt control; verified demo — rebuild triggered on the dev build against seeded history completes correctly (the 200k-op / 2GB-device gate is task 27, CHAOS-08 volume is task 26).
- **Queries + live update (04 §8 box 5):** `listNotes` cursor walk over > 2 pages (no dups/omissions, `createdAt.desc` default, terminates `nextCursor: null`); archived notes excluded by default, included with the filter; `limit > 100` → `VALIDATION_FAILED`; `getNote` returns the row or `ENTITY_NOT_FOUND`. Live update: with a subscribed `listNotes`, apply a remote op via the pull path → the subscription re-emits with the new row (04 §7 invalidation rule), asserted without UI polling.
- **Media attach (04 §8 box 8):** integration test — task-18 capture produces a MediaItem; `createNote` with its `mediaId` emits a v2 op whose payload carries it; projection row has `media_id` set; the op syncs/applies independently of upload completion. NoteDetail renders the thumbnail; NotesList shows the attachment glyph.
- **Screens (design-system §8.6):** all three screens ship all four §5 states (review-wave gate); archive goes through ConfirmSheet; editor save is optimistic (returns to list instantly, no spinner); rejected-op danger banner renders on NoteDetail. Verified demo checklist covering each.
- **i18n (04 §8 box 7):** `{id,en}.json` contain every `notes.*` key from ui-labels with the exact strings, no missing/extra keys in either locale (07-i18n CI completeness gate passes); verified demo — ID/EN toggle live-switches all three screens.
- **CHAOS-\* ownership:** CHAOS-01, CHAOS-06, CHAOS-07, CHAOS-08 use this module as workload and land with task 26 (`@bolusi/harness`); this task ships their module-scale equivalents above plus the §3.2 testability prerequisites so 26 needs no notes changes.
- **Lint/CI gates:** `bolusi/no-hardcoded-strings` clean over `packages/modules/notes/screens`; `bolusi/boundaries` — `apps/server` imports only `@bolusi/modules/notes` (never `*/screens`), screens import only from `apps/mobile`; `bolusi/no-float-money` clean; dual-dialect applier suite added to the standard `pnpm test` CI stage (no new stage); `tsc -b` composite build clean.

## REGISTRATION REQUIRED (task 49 landed the seam, 2026-07-15)

Task 49 built the server projection-apply step and the **one** registration list it folds from: `SERVER_MODULES` in `apps/server/src/deps.ts`. It is **empty at v0 by design**, and `registerModules(SERVER_MODULES)` derives BOTH the op validators and the projection appliers from it, so they can never name different module sets.

**This task's `defineModule` result MUST be appended to `SERVER_MODULES`, or the server folds nothing** — the op is accepted and its `operations` row is written, but its projection table stays empty in production, silently. That is the exact handoff-ring that left this unbuilt through 8 tasks (task 49's finding). Shipping the applier without registering it is a half-fix that looks done and folds nothing.

**Falsify the registration** (§2.11): with your module registered, push an op through the REAL push path (`processPushBatch`, not a hand-seeded row — T-14b) and assert the projection row appears; then remove your line from `SERVER_MODULES` and watch it go RED. A test that INSERTs its own projection row proves nothing about the fold.