# TASK 144 — the rest of the Sync Status screen: a green-guarded decoy, the same hardcode task 126 fixed still live on a second field, and a screen-reader label that never changes

**Priority:** MEDIUM — one live §2.11 decoy (a model value with zero callers, guarded by a sound test), one latent instance of task 126's exact bug, and two conformance defects. Filed by the task-126 implementer, who correctly refused to fold them in.
**Depends on:** 126, 15
**Blocks:** —
**SEC ids owned by THIS task:** none.
**Filed by:** the task-126 implementer, 2026-07-22.

> **PARTIAL — ITEM 2 DONE 2026-08-05.** `sync-manual-error` no longer hardcodes NETWORK: the screen now renders `translateErrorCode(input.manualSyncError)` (07-i18n §4.2 — the same derived lookup task 126 used) and shows the raw code beneath it (`sync-manual-error-code`) so a shop owner can quote it to support (§5). **Semantics DECIDED:** `manualSyncError` is a BARE `core.errors.*` code (e.g. `NETWORK`/`AUTH_TOKEN_INVALID`), NOT a full key — `translateErrorCode` keys `core.errors.<code>`; the model field doc + `model.test.ts`'s value updated to a bare code to remove the disagreement. **Positive control (§2.11, mandated):** a render test feeds two DIFFERENT codes and asserts the block renders DIFFERENT text (T-4 — two real renders, not asserted copy) + the raw code is shown; **falsified** — reverting to `t('core.errors.NETWORK')` makes both codes identical and reds `not.toBe`, and drops the code line. No producer change (they still pass `null`; the latent consumer bug is fixed for when one doesn't). mobile 818/818, lint + typecheck green.
>
> **ITEM 1 DONE 2026-08-05** (the decoy). `ManualSync.disabled.reasonKey` (a full catalog key the screen never read — it hardcoded `translateRejectionCode('DEVICE_REVOKED')`, so the model value had ZERO callers) is now `reasonCode` — a BARE `core.rejection.*` code — and `SyncStatusScreen` renders `translateRejectionCode(sync.reasonCode)`, the same shape the rejected rows use. Decision: a bare code (not the "generated key union" the filer guessed), because that is how every other rejection code in this screen is rendered. **§2.11 (the filer's exact acceptance — "breaking the SCREEN reds a test, which is precisely what does not happen today"):** a render test pins the disabled-reason to `translateRejectionCode(manualSync(revoked).reasonCode)`; **falsified** — hardcoding a DIFFERENT code (`CHAIN_HALTED`) in the screen reds it. The model producing a value the screen ignores is now impossible.
>
> **ITEM 3 DONE 2026-08-05** (the duplicate reassurance). `REASSURANCE_KEY.attention` was `sync.rejected.banner`, so the tier-1 reassurance line and the count banner rendered the SAME string ~40px apart — the second copy telling the user to tap to view the screen they were already on. It is now a NEW key `sync.status.attentionReassurance` — copy DECISION: a data-safety statement ("Perubahan yang ditolak tetap tersimpan di sini." / "Rejected changes are still saved here."), which answers the reassurance's question ("is my work safe?") rather than repeating the banner's "tap to view" prompt. Added the row to `ui-labels.md` (the seed source) → `i18n:seed`/`gen`/`check` (10 gates green); the `{count}` arg was dropped from the attention branch of `reassuranceText` (the reassurance no longer counts — the banner + section header do). **§2.11:** a render test asserts the reassurance text `not.toBe` the banner text; **falsified** — restoring the full pre-fix state (key `sync.rejected.banner` + the `{count}` arg) makes them equal and reds it. **REMAINING:** item 4 (SyncChip a11y label — contended `@bolusi/ui`, 6 callers), item 5 (the `syncing` state's missing visual-matrix row + the screenshot-count doc updates).

## Items

1. **`sync-disabled-reason` is a live decoy.** `SyncStatusScreen` hardcodes `translateRejectionCode('DEVICE_REVOKED')`, while `model.ts:283` computes `reasonKey: 'core.rejection.DEVICE_REVOKED'` and `model.test.ts:321` **asserts that computed value**. `grep reasonKey SyncStatusScreen.tsx` → nothing. The model produces a value with **zero callers** and a sound test guards it: break the screen's string and nothing reds. Wiring it means narrowing `ManualSync.reasonKey` from `string` to the generated key union — a type change, which is why 126 left it.
2. **`sync-manual-error` is task 126's bug, still live.** The screen renders `t('core.errors.NETWORK')` for **any** `manualSyncError`. Latent only because every producer currently passes `null` (`bootstrap/shell-inputs.ts:47`, `web/seed.ts:130`) — the moment one doesn't, every failure claims a network problem. **Decide the semantics first:** `model.test.ts:336` treats the field as a full **key** (`'core.errors.NETWORK'`) while `translateErrorCode` expects a bare **code**; they disagree today. Also design-system §5's Error state requires the code be shown for support, and this branch shows none.
3. **The `attention` reassurance line duplicates the banner verbatim.** `REASSURANCE_KEY.attention = 'sync.rejected.banner'`, so the screen shows "1 perubahan ditolak server. Ketuk untuk melihat." **twice**, ~40 px apart — and the second copy tells the user to tap to view the screen they are already on.
4. **`SyncChip`'s `accessibilityLabel` is state-invariant** — always `sync.status.lastSynced`, so a screen-reader user hears "Terakhir terhubung 4 menit lalu" whether the chip reads `synced` or `attention`. design-system §6.3/§6.4. Likely a `packages/ui` change (contended — serialize).
5. **The `syncing` state has never been screenshotted.** `e2e-web/visual.spec.ts:64-68`'s matrix has no `sync-status/syncing` row, so the fifth state's rendering has never been seen. 126 added render-test coverage but left the matrix alone (adding a row changes the screenshot count other docs cite — update those in the same change).

## FALSIFY (§2.11 — REPORT it)
For item 1, the acceptance is that breaking the SCREEN reds a test — that is precisely what does not happen today. For item 2, a positive control is mandatory: a non-network failure must resolve a different key than a network one, or the fix is untestable by construction.
